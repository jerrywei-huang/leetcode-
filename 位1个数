"""
编写一个函数，输入是一个无符号整数（以二进制串的形式），
返回其二进制表达式中数字位数为 '1' 的个数（也被称为汉明重量）。
"""

class Solution:
    def hammingWeight(self,n:int) -> int:
        #python中bin函数来获得一个整数的二进制字符串
        #二进制字符串以“0b”开头
        return bin(n).count("1")
