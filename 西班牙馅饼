"""
现在我们看见了这个西班牙馅饼长什么样，西班牙馅饼是个矩形形状，想不到吧。我们可以把它抽象成一个 
 行 
 列的正整数矩阵。每个格子有一个“美味值”，由于一些不可告人的原因我们对于这个馅饼没行只能吃一口，港岛妹妹说如果她吃到美味值为 
 的馅饼，就会给我做出美味值为 
 的红烧肉。问红烧肉最多能有多好吃。

输入格式N，M：表示 N行 M 列，馅饼的尺寸大小。
以下 N行 M列为馅饼每个格子的美味值。

输入中所有数是不超过 100的正整数。

将每行的最大值加起来
"""

row,col = map(int,input().split())
sum = 0
for _ in range(row):
    #设置为负无穷,无穷不可以设置为int
    maxx = float('-inf')
    roww = list(map(int,input().split()))
    #遍历每一行，选出最大值
    for i in roww:
        maxx = max(i,maxx)
    sum += maxx
print(sum)
